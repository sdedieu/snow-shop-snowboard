'use strict';

describe('app.snowboard module', function() {

  beforeEach(module('app.snowboard'));

  describe('snowboard controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var SnowboardCtrl = $controller('SnowboardCtrl');
      expect(SnowboardCtrl).toBeDefined();
    }));

  });
});