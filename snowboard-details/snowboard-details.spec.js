'use strict';

describe('app.snowboardDetails module', function() {

  beforeEach(module('app.snowboardDetails'));

  describe('SnowboardDetails controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var SnowboardDetailsCtrl = $controller('SnowboardDetailsCtrl');
      expect(SnowboardDetailsCtrl).toBeDefined();
    }));

  });
});